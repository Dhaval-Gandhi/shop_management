- if items.present?
  - items.each do |item|
    tr
      td
        = item.name
      td
        = item.rate
      td
        = link_to 'Select', 'javascript:void;', class: 'btn btn-sm btn-primary'
- else
  tr
    td.text-danger[colspan="3"]
      | There is no item.